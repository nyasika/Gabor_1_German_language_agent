# Másold le ezt a fájlt `secrets.toml` névvel (ugyanebbe a mappába),
# és add meg a saját Anthropic API kulcsodat.
# A secrets.toml SOHA nem kerül fel GitHub-ra (.gitignore védi).

ANTHROPIC_API_KEY = "sk-ant-..."
